# 游乐园照片查找系统 - 现代化重构版本

## 项目简介

这是一个游乐园照片查找系统的现代化重构版本，采用前后端分离架构，提供了更好的用户体验和可维护性。

### 主要功能

- 📸 **照片查找**: 按时间、地点、穿着特征等多维度搜索
- 🤖 **AI智能匹配**: 上传自拍照进行人脸识别匹配
- 🛒 **购物车管理**: 完整的购物车功能和结算流程
- 💳 **在线支付**: 支持多种支付方式
- 📱 **响应式设计**: 完美适配移动端、平板和桌面端
- 🎨 **现代化UI**: 采用Tailwind CSS构建的现代界面

## 技术栈

### 前端
- **框架**: React 18
- **路由**: React Router 6
- **样式**: Tailwind CSS 3
- **图标**: Lucide React
- **构建工具**: Vite
- **HTTP客户端**: Axios

### 后端
- **框架**: Express.js
- **认证**: JWT (JSON Web Tokens)
- **文件上传**: Multer
- **密码加密**: bcryptjs
- **安全**: Helmet, CORS
- **日志**: Morgan

## 项目结构

```
amusement_photo_finder_modern/
├── frontend/                 # 前端React应用
│   ├── public/               # 静态资源
│   ├── src/                  # 源代码
│   │   ├── components/       # 组件
│   │   ├── pages/            # 页面
│   │   ├── context/          # React Context
│   │   ├── utils/            # 工具函数
│   │   └── assets/           # 资源文件
│   ├── package.json
│   └── vite.config.js
├── backend/                  # 后端Express服务
│   ├── src/                  # 源代码
│   │   ├── controllers/      # 控制器
│   │   ├── routes/           # 路由
│   │   ├── models/           # 数据模型
│   │   ├── middleware/       # 中间件
│   │   └── utils/            # 工具函数
│   ├── uploads/              # 文件上传目录
│   ├── package.json
│   └── .env
└── README.md
```

## 快速开始

### 1. 环境要求

- Node.js 16.x 或更高版本
- npm 8.x 或更高版本

### 2. 安装依赖

#### 前端安装
```bash
cd frontend
npm install
```

#### 后端安装
```bash
cd backend
npm install
```

### 3. 配置环境变量

在 `backend/.env` 文件中配置以下环境变量：

```env
PORT=3001
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

### 4. 启动项目

#### 启动后端服务
```bash
cd backend
npm run dev
```

#### 启动前端开发服务器
```bash
cd frontend
npm run dev
```

### 5. 访问应用

- 前端应用: http://localhost:5173
- 后端API: http://localhost:3001
- 健康检查: http://localhost:3001/health

## API接口文档

### 认证相关
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `GET /api/auth/me` - 获取当前用户信息
- `PUT /api/auth/me` - 更新用户信息
- `POST /api/auth/refresh` - 刷新Token

### 照片相关
- `GET /api/photos` - 获取照片列表
- `GET /api/photos/popular` - 获取热门项目
- `GET /api/photos/:id` - 获取照片详情
- `POST /api/photos/search` - 搜索照片
- `POST /api/photos/upload-selfie` - 上传自拍照进行AI匹配
- `GET /api/photos/:id/download` - 下载照片

### 购物车相关
- `GET /api/cart` - 获取购物车
- `POST /api/cart` - 添加商品到购物车
- `PUT /api/cart/items/:itemId` - 更新购物车商品
- `DELETE /api/cart/items/:itemId` - 移除购物车商品
- `DELETE /api/cart` - 清空购物车

## 生产环境部署

### 构建前端
```bash
cd frontend
npm run build
```

### 启动后端服务
```bash
cd backend
npm start
```

## 安全注意事项

1. 在生产环境中，请使用强密码和安全的JWT密钥
2. 配置正确的CORS源，避免使用通配符
3. 定期更新依赖包以修复安全漏洞
4. 考虑使用HTTPS协议
5. 实现速率限制以防止暴力攻击

## 性能优化建议

1. 使用CDN加速静态资源
2. 实现图片懒加载
3. 配置适当的缓存策略
4. 考虑使用Redis缓存热点数据
5. 实现数据库索引优化查询性能

## 许可证

MIT License